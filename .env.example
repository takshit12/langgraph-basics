# Copy to .env and fill in. Only needed for the LLM-backed paths
# (simple_graph.py's search branch and the optional Studio question node).
# The state_flow_demo.py and memory_demo.py examples need NO key.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4.1-mini
